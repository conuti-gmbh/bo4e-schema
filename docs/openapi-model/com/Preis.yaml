components:
  schemas:
    Preis:
      title: Preis
      type: object
      properties:
        wert:
          $ref: ../fields/com/Preis/wert.yaml#/components/schemas/wert
        menge:
          $ref: ../fields/com/Preis/menge.yaml#/components/schemas/menge
        minimaleMenge:
          $ref: >-
            ../fields/com/Preis/minimaleMenge.yaml#/components/schemas/minimaleMenge
        maximaleMenge:
          $ref: >-
            ../fields/com/Preis/maximaleMenge.yaml#/components/schemas/maximaleMenge
        einheit:
          $ref: ../fields/com/Preis/einheit.yaml#/components/schemas/einheit
        bezugswert:
          $ref: ../fields/com/Preis/bezugswert.yaml#/components/schemas/bezugswert
        status:
          $ref: ../fields/com/Preis/status.yaml#/components/schemas/status
        preisart:
          $ref: ../fields/com/Preis/preisart.yaml#/components/schemas/preisart
