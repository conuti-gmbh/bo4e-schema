components:
  schemas:
    Abschlag:
      title: Abschlag
      type: object
      properties:
        typ:
          $ref: ../fields/com/Abschlag/typ.yaml#/components/schemas/typ
        prozent:
          $ref: ../fields/com/Abschlag/prozent.yaml#/components/schemas/prozent
