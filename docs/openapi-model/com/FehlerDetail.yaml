components:
  schemas:
    FehlerDetail:
      title: FehlerDetail
      type: object
      properties:
        code:
          $ref: ../fields/com/FehlerDetail/code.yaml#/components/schemas/code
        ursache:
          $ref: ../fields/com/FehlerDetail/ursache.yaml#/components/schemas/ursache
        beschreibung:
          $ref: >-
            ../fields/com/FehlerDetail/beschreibung.yaml#/components/schemas/beschreibung
