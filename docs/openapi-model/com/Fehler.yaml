components:
  schemas:
    Fehler:
      title: Fehler
      type: object
      properties:
        typ:
          $ref: ../fields/com/Fehler/typ.yaml#/components/schemas/typ
        fehlerDetails:
          $ref: >-
            ../fields/com/Fehler/fehlerDetails.yaml#/components/schemas/fehlerDetails
