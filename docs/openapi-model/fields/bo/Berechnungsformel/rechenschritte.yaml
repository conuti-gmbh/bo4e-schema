components:
  schemas:
    rechenschritte:
      type: array
      items:
        $ref: ../../../com/Rechenschritt.yaml#/components/schemas/Rechenschritt
      description: >-
        Eine Berechnungsformel enthält, falls sie notwendig ist, einen oder
        mehrere Berechnungschritte, die hier rekursiv abgebildet werden.
      nullable: true
