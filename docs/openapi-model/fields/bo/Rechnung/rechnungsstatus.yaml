components:
  schemas:
    rechnungsstatus:
      $ref: ../../../enum/Rechnungsstatus.yaml#/components/schemas/Rechnungsstatus
      description: >-
        Status der Rechnung zur Kennzeichnung des Bearbeitungsstandes. Details
        siehe ENUM Rechnungsstatus
