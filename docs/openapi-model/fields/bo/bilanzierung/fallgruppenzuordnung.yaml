components:
  schemas:
    fallgruppenzuordnung:
      $ref: >-
        ../../../enum/Fallgruppenzuordnung.yaml#/components/schemas/Fallgruppenzuordnung
      description: Fallgruppenzuordnung (für gas RLM)
