components:
  schemas:
    pruefgegenstand:
      type: string
      description: >-
        Das geprüfte Dokument, z.B. die Referenz auf die EDIFACT-Nachricht die
        geprüft / beanstandet wurde
