components:
  schemas:
    varianten:
      type: array
      items:
        $ref: >-
          ../../../com/Angebotsvariante.yaml#/components/schemas/Angebotsvariante
      description: >-
        Eine oder mehrere Varianten des Angebots mit den Angebotsteilen. Ein
        Angebot besteht mindestens aus einer Variante.
      nullable: true
