components:
  schemas:
    zeitbasis:
      $ref: ../../../enum/Zeiteinheit.yaml#/components/schemas/Zeiteinheit
      description: >-
        Die Zeit(dauer) auf die sich der Preis bezieht. Z.B. ein Jahr für einen
        Leistungspreis der in €/kW/Jahr

        ausgegeben wird.
