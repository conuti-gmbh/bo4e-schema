components:
  schemas:
    Fallgruppenzuordnung:
      title: Fallgruppenzuordnung
      type: string
      enum:
        - GABI_RLMmT
        - GABI_RLMoT
        - GABI_RLMNEV
      description: Fallgruppenzuordnung
