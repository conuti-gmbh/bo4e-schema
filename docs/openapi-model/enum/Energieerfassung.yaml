components:
  schemas:
    Energieerfassung:
      title: Energieerfassung
      type: string
      enum:
        - SEPARAT_ERFASSEN
        - NICHT_SEPARAT_ERFASSEN
      description: Energieerfassung
